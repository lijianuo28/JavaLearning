# Java Learning
这是我的Java学习代码仓库，包含基础练习和项目。

mvn compile exec:java -Dexec.mainClass="JdbcTest" -q 
这一行是运行JDBC代码的执行命令，可以去掉Maven的日志。

mvn compile
mvn exec:java -Dexec.mainClass="JdbcTest"
这两行是包含Maven日志的执行命令。